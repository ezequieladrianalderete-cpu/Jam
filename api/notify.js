// Vercel Serverless Function - JAM 2026// POST /api/notify { id: "uuid" }const RESEND_KEY = process.env.RESEND_API_KEY;const SB_URL = process.env.SUPABASE_URL || 'https://paexpktmcytccjphoppu.supabase.co';const SB_KEY = process.env.SUPABASE_SERVICE_KEY;const SITE_URL = process.env.SITE_URL || 'https://jam-inscripciones.vercel.app';const CORS = {  'Access-Control-Allow-Origin': '*',  'Access-Control-Allow-Methods': 'POST, OPTIONS',  'Access-Control-Allow-Headers': 'Content-Type'};async function sbFetch(path, opts = {}) {  const r = await fetch(SB_URL + '/rest/v1' + path, {    ...opts,    headers: { apikey: SB_KEY, Authorization: 'Bearer ' + SB_KEY, 'Content-Type': 'application/json', Prefer: 'return=representation', ...(opts.headers || {}) }  });  return r.json();}async function getQR(text) {  const r = await fetch('https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=' + encodeURIComponent(text) + '&format=png');  return Buffer.from(await r.arrayBuffer()).toString('base64');}async function sendMail(to, subject, html) {  const r = await fetch('https://api.resend.com/emails', {    method: 'POST',    headers: { Authorization: 'Bearer ' + RESEND_KEY, 'Content-Type': 'application/json' },    body: JSON.stringify({ from: 'JAM 2026 <onboarding@resend.dev>', to: [].concat(to), subject, html })  });  return r.json();}function buildHTML(rec, qr) {
